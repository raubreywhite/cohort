% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_integration.R
\name{add_data_from_cohort}
\alias{add_data_from_cohort}
\title{Add cohort artifacts to a \code{plnr::Plan} in bulk}
\usage{
add_data_from_cohort(plan, cp, cohort, artifacts = NULL, prefix = "")
}
\arguments{
\item{plan}{A \code{plnr::Plan} instance.}

\item{cp}{A \code{CohortPipeline} instance.}

\item{cohort}{Character. Name of the cohort to draw artifacts from.}

\item{artifacts}{Character vector. Artifact names on \code{cohort} to
register. Defaults to every artifact attached to that cohort.}

\item{prefix}{Character. Prefix prepended to the data-entry name on
the plan. Defaults to \code{""}. The full entry name is
\code{paste0(prefix, cohort, "__", artifact)}.}
}
\value{
The \code{plan} (invisibly), modified in place.
}
\description{
Bulk-ingest one or more cached artifacts from a \code{CohortPipeline} cohort
into a \code{plnr::Plan} as named data entries. This replaces the typical
nested for-loop that calls \code{plan$add_data(name, direct = ...)} once per
(cohort, artifact) pair.
}
\details{
For each artifact name supplied, the helper retrieves it from the
specified cohort and registers it on the plan via
\code{plan$add_data(name, direct = ...)}. The data-entry name on the plan
is composed as \code{paste0(prefix, cohort, "__", artifact)}.

This function is the only point of contact between \code{cohort} and the
\code{plnr} package. \code{plnr} is declared in \code{Suggests}, not \code{Imports}, so
you must have it installed to use this helper.
}
\examples{
if (requireNamespace("plnr", quietly = TRUE) &&
    requireNamespace("data.table", quietly = TRUE)) {
  library(data.table)

  d <- data.table(id = 1:6, age = c(20, 30, 40, 50, 60, 70))
  cp <- CohortPipeline$new()
  cp$load(d)
  cp$exclude_and_track("root", "Under 30", "age < 30")

  cp$set_artifact("dt",
    from = "root",
    fn = function(dt, sib) dt
  )
  cp$set_artifact("n",
    from = "root",
    fn = function(dt, sib) nrow(dt)
  )

  plan <- plnr::Plan$new()
  add_data_from_cohort(plan, cp, cohort = "root")
  plan$get_data()
}

}
\seealso{
\link{CohortPipeline}
}
